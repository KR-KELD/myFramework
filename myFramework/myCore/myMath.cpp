#include "myMath.h"

const float myMath::PI = 3.141592654f;
const float myMath::EPSILON = 0.000001f;
const float myMath::PI2M = PI * 2;
const float myMath::PI2D = float(PI / 2.0f);
const float myMath::PI4D = float(PI / 4.0f);
const float myMath::PI8D = float(PI / 8.0f);
const float myMath::PI16D = float(PI / 16.0f);
const float myMath::PI32D = float(PI / 32.0f);
const float myMath::PI64D = float(PI / 64.0f);
const float myMath::PI128D = float(PI / 128.0f);